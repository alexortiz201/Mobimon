import test from 'tape';
import helpers from '../../utils/test/test.helpers';

test('Create tests for Login Page', assert => helpers.createFailingTest(assert));


/*
test('Login', nest => {
  nest.test('...login form', assert => {
    const msg = 'Login should render the login form.';
  });
});
*/
