/**
 * This need to export a path, and the self contained component
 *
 * const Route = {
 *   path: '/incubator',
 *   component: Incubator
 * }
 */

/*
const path = '/incubator';
const deps = [React];
const Incubator = createIncubator(...deps);
const Route = {
  path,
  component: Incubator,
};

export default Route;
*/
